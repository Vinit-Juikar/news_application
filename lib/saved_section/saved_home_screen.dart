import 'package:flutter/cupertino.dart';

class SavedHomeScreen extends StatelessWidget {
  const SavedHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("this is saved section"),);
  }
}